Процедура ОбработкаПроведения(Отказ, РежимПроведения)

    // Очищаем движения по всем регистрам (накопления и бухгалтерии)
    Движения.ОстаткиМатериалов.Записать();
    Движения.СтоимостьМатериалов.Записать();
    Движения.Управленческий.Записать();

    // Перебираем строки табличной части "Материалы"
    Для Каждого Строка Из Материалы Цикл

        // ----- Регистры накопления (приход) -----
        // ОстаткиМатериалов
        ДвижениеОст = Движения.ОстаткиМатериалов.Добавить();
        ДвижениеОст.ВидДвижения = ВидДвиженияНакопления.Приход;
        ДвижениеОст.Период = Дата;
        ДвижениеОст.Материал = Строка.Материал;
        ДвижениеОст.Количество = Строка.Количество;
        ДвижениеОст.НаборСвойств = Строка.НаборСвойств;   // из задания 14

        // СтоимостьМатериалов
        ДвижениеСтоим = Движения.СтоимостьМатериалов.Добавить();
        ДвижениеСтоим.ВидДвижения = ВидДвиженияНакопления.Приход;
        ДвижениеСтоим.Период = Дата;
        ДвижениеСтоим.Материал = Строка.Материал;
        ДвижениеСтоим.Стоимость = Строка.Сумма;

        // ----- Бухгалтерский регистр "Управленческий" (проводки) -----
        // Дебет 41 "Товары" – Кредит 60 "Расчеты с поставщиками"
        ДвижениеБух = Движения.Управленческий.Добавить();
        ДвижениеБух.Период = Дата;
        ДвижениеБух.СчетДт = ПланыСчетов.Основной.Товары;              // предопределенный счет 41
//        ДвижениеБух.СубконтоДт = Строка.Материал;                     // первое субконто (Материалы) = номенклатура
        ДвижениеБух.СчетКт = ПланыСчетов.Основной.РасчетыСПоставщиками; // предопределенный счет 60
        ДвижениеБух.Сумма = Строка.Сумма;
       
		

    КонецЦикла;

    // Сохраняем движения всех регистров
    Движения.ОстаткиМатериалов.Записать();
    Движения.СтоимостьМатериалов.Записать();
    Движения.Управленческий.Записать();

КонецПроцедуры
