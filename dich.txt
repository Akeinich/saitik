Процедура ОбработкаПроведения(Отказ, РежимПроведения)

    // Очищаем движения перед записью
    Движения.ОстаткиМатериалов.Записать();
    Движения.СтоимостьМатериалов.Записать();
    
    // Перебираем строки табличной части ПЕРЕЧЕНЬНОМЕНКЛАТУРЫ (а не СписокНоменклатуры)
    Для Каждого Строка Из ПереченьНоменклатуры Цикл
        
        // === Регистр 1: ОстаткиМатериалов (только для материалов) ===
        Если Строка.Номенклатура.ВидНоменклатуры = Перечисления.ВидыНоменклатуры.Материал Тогда
            ДвижениеОст = Движения.ОстаткиМатериалов.Добавить();
            ДвижениеОст.ВидДвижения = ВидДвиженияНакопления.Приход;
            ДвижениеОст.Период = Дата;
            ДвижениеОст.Материал = Строка.Номенклатура;
            ДвижениеОст.Количество = Строка.Количество;
        КонецЕсли;
        
        // === Регистр 2: СтоимостьМатериалов (только для материалов) ===
        Если Строка.Номенклатура.ВидНоменклатуры = Перечисления.ВидыНоменклатуры.Материал Тогда
            ДвижениеСтоим = Движения.СтоимостьМатериалов.Добавить();
            ДвижениеСтоим.ВидДвижения = ВидДвиженияНакопления.Приход;
            ДвижениеСтоим.Период = Дата;
            ДвижениеСтоим.Материал = Строка.Номенклатура;
            // Важно: стоимость берем из реквизита Сумма или создайте отдельный реквизит Стоимость в ТЧ
            ДвижениеСтоим.Стоимость = Строка.Сумма;
        КонецЕсли;
        
    КонецЦикла;

КонецПроцедуры