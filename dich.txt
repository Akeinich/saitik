Процедура ПолучитьПредставление(ТекстПредставления, ДанныеДляФормирования, СтандартнаяОбработка)
    Если ДанныеДляФормирования.Свойство("Ссылка") Тогда
        Ссылка = ДанныеДляФормирования.Ссылка;
        Вид = Ссылка.ВидНоменклатуры;
        ТекстПредставления = Строка(Ссылка.Наименование) + " (" + Строка(Вид) + ")";
        СтандартнаяОбработка = Ложь;
    КонецЕсли;
КонецПроцедуры
